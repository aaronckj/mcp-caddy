# mcp-caddy

MCP server for Caddy web server management — routes, upstreams, certificates, reload.

Provides tools to manage Caddy instances via the admin API, enabling LLM-driven configuration of reverse proxies, load balancing, and certificate management.
